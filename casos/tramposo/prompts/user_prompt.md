Template (una llamada por ticket, orquestada entre los 5 sub-agentes):

```
Ticket: {TICKET_TEXTO}
```

Nota: aunque el README describe 5 sub-agentes con 3 modelos en paralelo,
el único parámetro real que recibe el sistema es el texto del ticket —
no hay más variables documentadas en ningún lado del repositorio.
